all:
  hosts:
    local:
      ansible_host: localhost
      ansible_connection: local
      ansible_user: devops-23
    vm_ips:
      gce:
        ansible_host: "{{ gce_ip }}" #34.171.26.52
        ansible_conection: ssh
        ansible_user: nsvenkerud
        ansible_become_user: root
      aws:
        ansible_host: "{{ aws_ip }}" #34.171.26.52
        ansible_conection: ssh
        ansible_user: ec2-user
        ansible_become_user: root

